import XCTest
@testable import CarrisMetropolitana

final class CarrisMetropolitanaTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CarrisMetropolitana().text, "Hello, World!")
    }
}
